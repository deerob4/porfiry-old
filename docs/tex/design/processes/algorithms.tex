\subsection{Calculations}
Much of the application will 

\subsubsection{Calculate House Points}
\begin{verbatim}
  function calculateHousePoints(attempts, correct) {
    set ``full'' to attempts where answer == correct and peek == false
    set ``half'' to attempts where answer == correct and peek == true
    set ``none'' to attempts where answer == false
  }
\end{vebatim}
